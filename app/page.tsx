import Link from "next/link";
import { ProductGrid } from "@/components/ProductGrid";
import { products } from "@/lib/products";
const categories=[['📱','Electronics'],['👟','Fashion'],['🛋️','Home & Living'],['💄','Beauty'],['🛒','Groceries'],['🧸','Kids & Toys']];
export default function Home(){return <main><div className="container"><section className="hero"><aside className="side-menu">{categories.map((c)=><Link href={`/products?category=${encodeURIComponent(c[1])}`} key={c[1] as string}>{c[0]} {c[1]}</Link>)}</aside><div className="hero-main"><div className="eyebrow">The marketplace for everyday life</div><h1>Find your next favourite thing.</h1><p>Great brands, trusted sellers, and seriously good prices—all in one place.</p><Link className="cta" href="/products">Shop the collection →</Link></div></section><section className="section"><h2 className="section-title">Shop by category</h2><div className="category-grid">{categories.map(([icon,name])=><Link className="category-card" href={`/products?category=${encodeURIComponent(name)}`} key={name}><span className="category-icon">{icon}</span>{name}</Link>)}</div></section><section className="section"><div className="section-head"><h2 className="section-title">Deals worth seeing</h2><Link className="see-all" href="/products">See all deals →</Link></div><ProductGrid products={products.slice(0,4)} /></section><section className="section"><div className="section-head"><h2 className="section-title">Popular right now</h2><Link className="see-all" href="/products">Explore all →</Link></div><ProductGrid products={products.slice(4)} /></section></div></main>}
